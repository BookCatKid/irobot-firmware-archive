name: Historical firmware backfill

on:
  workflow_dispatch:
    inputs:
      family:
        description: "Internal firmware family (for example sapphire)"
        required: true
        default: "sapphire"
      template:
        description: "Direct firmware URL template"
        required: true
        default: "https://prod-ota-firmware.iot.irobotapi.com/{family}-{version}.signed"
      scheme:
        description: "Version-number scheme"
        type: choice
        options: [classic, semantic, numeric]
        default: classic
      year_start:
        description: "Classic scheme first YY"
        default: "17"
      year_end:
        description: "Classic scheme last YY"
        default: "26"
      major_max:
        description: "Semantic scheme highest major version"
        default: "15"
      minor_max:
        description: "Semantic scheme highest minor version"
        default: "60"
      patch_max:
        description: "Highest patch number to probe"
        default: "15"
      numeric_start:
        description: "Numeric scheme first token"
        default: "0"
      numeric_end:
        description: "Numeric scheme last token"
        default: "9999"
      numeric_width:
        description: "Numeric scheme zero-pad width; 0 means no padding"
        default: "0"
      version_prefix:
        description: "Optional catalog version prefix for numeric tokens (for example v)"
        default: ""
      max_probes:
        description: "Maximum object probes; 0 means unlimited"
        default: "0"
      archive_found:
        description: "Also download/analyze/upload found packages"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: firmware-catalog
  cancel-in-progress: false

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
      - name: Validate catalog JSON
        run: python -m json.tool data/catalog.json >/dev/null
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y squashfs-tools
          python -m pip install -e .
      - name: Probe historical objects
        shell: bash
        run: |
          cap="${{ inputs.max_probes }}"
          extra=()
          if [[ "$cap" != "0" ]]; then extra+=(--max-probes "$cap"); fi
          irobot-fw backfill \
            --family "${{ inputs.family }}" \
            --template '${{ inputs.template }}' \
            --scheme "${{ inputs.scheme }}" \
            --year-start "${{ inputs.year_start }}" \
            --year-end "${{ inputs.year_end }}" \
            --major-max "${{ inputs.major_max }}" \
            --minor-max "${{ inputs.minor_max }}" \
            --patch-max "${{ inputs.patch_max }}" \
            --numeric-start "${{ inputs.numeric_start }}" \
            --numeric-end "${{ inputs.numeric_end }}" \
            --numeric-width "${{ inputs.numeric_width }}" \
            --version-prefix "${{ inputs.version_prefix }}" \
            --workers 4 --pause 0.02 "${extra[@]}"
      - name: Archive newly found packages
        if: ${{ inputs.archive_found == true }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: irobot-fw archive --repo "$GITHUB_REPOSITORY" --family "${{ inputs.family }}"
      - name: Rebuild embedded auxiliary firmware index
        run: python scripts/build_auxiliary_index.py
      - name: Rebuild embedded audio index
        run: python scripts/build_audio_index.py
      - name: Snapshot official release-note evidence
        run: python scripts/snapshot_release_note_evidence.py
      - name: Rebuild completeness ledger
        run: python scripts/build_completeness_ledger.py
      - name: Verify archived Release integrity
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/audit_release_integrity.py --repo "$GITHUB_REPOSITORY"
      - name: Commit results
        run: |
          git config user.name "irobot-firmware-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/catalog.json data/firmware data/auxiliary-firmware.json data/audio-assets.json data/completeness.json data/research/official-release-note-artifact-gaps-current.json
          if ! git diff --cached --quiet; then
            git commit -m "archive: backfill ${{ inputs.family }} firmware"
            git fetch origin main
            git rebase origin/main
            git push origin HEAD:main
          fi
